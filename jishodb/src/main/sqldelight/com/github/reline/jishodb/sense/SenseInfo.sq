CREATE TABLE IF NOT EXISTS SenseInfo(
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    value TEXT NOT NULL
);

insertSenseInfo:
INSERT INTO SenseInfo (value)
VALUES (?);

rowid:
SELECT last_insert_rowid();